export const CSS = String.raw`
:root{--ink:#131613;--muted:#646b62;--paper:#f1f0e8;--card:#faf9f2;--line:#c9c9bb;--acid:#c8f135;--red:#c6472f;--dark:#20251f;--mono:"SFMono-Regular",Consolas,"Liberation Mono",monospace;--sans:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:light}
*{box-sizing:border-box}html{background:var(--paper);color:var(--ink);font-family:var(--sans)}body{margin:0;min-height:100vh;background-image:linear-gradient(rgba(19,22,19,.045) 1px,transparent 1px),linear-gradient(90deg,rgba(19,22,19,.045) 1px,transparent 1px);background-size:24px 24px}a{color:inherit}button,input,textarea,select{font:inherit}.shell{width:min(1120px,calc(100% - 32px));margin:0 auto}.mast{display:flex;align-items:center;justify-content:space-between;padding:24px 0 20px;border-bottom:2px solid var(--ink)}.brand{display:flex;align-items:center;gap:11px;text-decoration:none;font-weight:850;letter-spacing:-.04em;font-size:21px}.brand-mark{display:grid;place-items:center;width:28px;height:28px;background:var(--acid);border:2px solid var(--ink);box-shadow:3px 3px 0 var(--ink);font:700 15px var(--mono)}.nav{display:flex;gap:18px;font:700 12px var(--mono);text-transform:uppercase;letter-spacing:.08em}.nav a{text-decoration:none}.nav a:hover{text-decoration:underline}.hero{padding:68px 0 56px;display:grid;grid-template-columns:1.5fr .7fr;gap:48px;align-items:end}.eyebrow,.section-label{font:700 11px var(--mono);text-transform:uppercase;letter-spacing:.14em;color:var(--muted)}h1{font-size:clamp(48px,8vw,96px);letter-spacing:-.075em;line-height:.85;margin:14px 0 25px;max-width:850px}.hero p{max-width:650px;margin:0;color:var(--muted);font-size:18px;line-height:1.6}.hero-stat{justify-self:end;border:2px solid var(--ink);padding:18px;background:var(--acid);box-shadow:7px 7px 0 var(--ink);min-width:180px}.hero-stat strong{display:block;font:800 48px/1 var(--mono)}.hero-stat span{font:700 11px var(--mono);text-transform:uppercase}.section-head{display:flex;align-items:center;justify-content:space-between;padding:18px 0;border-top:2px solid var(--ink);border-bottom:1px solid var(--line)}.identity-list{list-style:none;padding:0;margin:0 0 64px}.identity-row{display:grid;grid-template-columns:46px 1fr auto;align-items:center;gap:16px;padding:21px 4px;border-bottom:1px solid var(--line);text-decoration:none;transition:padding .15s,background .15s}.identity-row:hover{background:var(--card);padding-left:14px;padding-right:14px}.arrow{font:700 22px var(--mono)}.identity-name{font-weight:780;font-size:21px;letter-spacing:-.025em}.identity-meta{font:12px var(--mono);color:var(--muted)}.empty{padding:50px 0;color:var(--muted);font-family:var(--mono)}footer{border-top:2px solid var(--ink);padding:22px 0 40px;display:flex;justify-content:space-between;color:var(--muted);font:11px var(--mono)}
/* identity */
.profile{padding:56px 0 40px}.profile h1{font-size:clamp(45px,7vw,78px);margin-bottom:18px}.handle{display:inline-block;background:var(--dark);color:var(--paper);padding:7px 10px;font:700 13px var(--mono)}.description{max-width:720px;color:var(--muted);font-size:17px;line-height:1.6}.command-block{margin:12px 0 42px}.command{display:flex;align-items:center;gap:12px;background:var(--dark);color:#f6f4e9;border-left:7px solid var(--acid);padding:15px 16px;font:13px/1.5 var(--mono);overflow:auto}.command code{flex:1;white-space:nowrap}.copy{border:1px solid #71796d;background:transparent;color:inherit;padding:7px 10px;cursor:pointer;font:700 10px var(--mono);text-transform:uppercase}.copy:hover,.copy.copied{background:var(--acid);color:var(--ink);border-color:var(--acid)}.warning{font:11px/1.5 var(--mono);color:var(--muted);padding:10px 0}.key-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin:18px 0 64px}.key-card{background:var(--card);border:1px solid var(--line);padding:22px;box-shadow:4px 4px 0 rgba(19,22,19,.12)}.key-top{display:flex;justify-content:space-between;gap:12px;align-items:start;margin-bottom:28px}.key-type{background:var(--acid);border:1px solid var(--ink);padding:5px 7px;font:700 10px var(--mono);text-transform:uppercase}.key-date{font:11px var(--mono);color:var(--muted)}.key-card h2{font-size:22px;letter-spacing:-.035em;margin:0 0 8px}.comment{font:12px var(--mono);color:var(--muted);min-height:18px;overflow-wrap:anywhere}.fingerprint{margin:22px 0 16px;border-top:1px solid var(--line);padding-top:14px}.fingerprint span{display:block;font:700 9px var(--mono);text-transform:uppercase;color:var(--muted);margin-bottom:6px}.fingerprint code{font:11px/1.5 var(--mono);overflow-wrap:anywhere}.full-key{width:100%;text-align:left;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;background:#e7e6db;border:0;padding:10px;font:10px var(--mono)}
/* admin */
.admin-body{background-color:#e8e7dd}.admin-shell{width:min(1240px,calc(100% - 32px));margin:0 auto}.admin-head{padding:21px 0;border-bottom:2px solid var(--ink);display:flex;justify-content:space-between;align-items:center}.admin-title{font:800 18px var(--mono)}.status-dot{display:inline-block;width:8px;height:8px;border-radius:50%;background:var(--acid);border:1px solid var(--ink);margin-right:8px}.btn{border:1.5px solid var(--ink);background:var(--card);padding:9px 13px;font:700 11px var(--mono);text-transform:uppercase;cursor:pointer;box-shadow:2px 2px 0 var(--ink)}.btn:hover{transform:translate(1px,1px);box-shadow:1px 1px 0 var(--ink)}.btn.primary{background:var(--acid)}.btn.danger{color:var(--red);border-color:var(--red);box-shadow:2px 2px 0 var(--red)}.btn.small{padding:6px 8px;box-shadow:none}.admin-toolbar{display:flex;justify-content:space-between;align-items:end;padding:45px 0 20px}.admin-toolbar h1{font-size:clamp(36px,5vw,60px);margin:7px 0 0}.admin-grid{display:grid;grid-template-columns:1fr 330px;gap:20px;padding-bottom:60px}.panel{background:var(--card);border:1.5px solid var(--ink)}.panel-head{padding:13px 16px;background:var(--dark);color:var(--paper);display:flex;justify-content:space-between;align-items:center;font:700 11px var(--mono);text-transform:uppercase;letter-spacing:.08em}.admin-identity{border-bottom:1px solid var(--line)}.admin-identity:last-child{border-bottom:0}.admin-identity summary{list-style:none;cursor:pointer;padding:18px;display:grid;grid-template-columns:1fr auto;gap:12px}.admin-identity summary::-webkit-details-marker{display:none}.admin-identity summary strong{display:block;font-size:17px}.admin-identity summary span{font:11px var(--mono);color:var(--muted)}.visibility{align-self:center;padding:4px 6px;border:1px solid var(--line);font:700 9px var(--mono)!important;text-transform:uppercase}.identity-editor{padding:0 18px 20px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.field{display:grid;gap:6px;margin-bottom:12px}.field.wide{grid-column:1/-1}.field label{font:700 10px var(--mono);text-transform:uppercase;color:var(--muted)}.field input,.field textarea,.field select{width:100%;border:1px solid var(--line);background:#fff;padding:10px;outline:none}.field input:focus,.field textarea:focus,.field select:focus{border-color:var(--ink);box-shadow:0 0 0 2px var(--acid)}.actions{display:flex;gap:9px;flex-wrap:wrap}.key-admin-list{margin:18px 0;border-top:1px solid var(--line)}.key-admin{padding:12px 0;border-bottom:1px solid var(--line);display:grid;grid-template-columns:1fr auto;gap:10px}.key-admin strong{font-size:13px}.key-admin code{display:block;color:var(--muted);font:9px/1.5 var(--mono);overflow-wrap:anywhere}.audit{padding:0 14px;max-height:620px;overflow:auto}.audit-item{padding:13px 0;border-bottom:1px solid var(--line)}.audit-item strong{font:700 10px var(--mono);text-transform:uppercase}.audit-item p{margin:5px 0;font-size:12px;overflow-wrap:anywhere}.audit-item time{font:9px var(--mono);color:var(--muted)}.login-wrap{min-height:100vh;display:grid;place-items:center;padding:20px}.login{width:min(430px,100%);border:2px solid var(--ink);background:var(--card);box-shadow:9px 9px 0 var(--ink);padding:34px}.login h1{font-size:43px;margin:27px 0 10px}.login p{color:var(--muted);line-height:1.5}.login .btn{width:100%;padding:12px}.error{min-height:20px;color:var(--red);font:11px var(--mono)}dialog{border:2px solid var(--ink);background:var(--card);padding:0;width:min(580px,calc(100% - 30px));box-shadow:9px 9px 0 var(--ink)}dialog::backdrop{background:rgba(19,22,19,.65)}.dialog-body{padding:22px}.toast{position:fixed;right:20px;bottom:20px;max-width:360px;background:var(--dark);color:#fff;padding:13px 17px;border-left:6px solid var(--acid);font:12px var(--mono);z-index:20}.toast.bad{border-color:var(--red)}[hidden]{display:none!important}
@media(max-width:800px){.hero{grid-template-columns:1fr;padding-top:45px}.hero-stat{justify-self:start}.key-grid{grid-template-columns:1fr}.admin-grid{grid-template-columns:1fr}.audit-panel{order:-1}.form-grid{grid-template-columns:1fr}.field.wide{grid-column:auto}}
@media(max-width:520px){.shell,.admin-shell{width:min(100% - 20px,1120px)}.mast{align-items:flex-start}.nav{gap:10px}.hero{padding-bottom:40px}.identity-row{grid-template-columns:30px 1fr}.identity-meta{grid-column:2}.key-card{padding:16px}.command{align-items:flex-start}.admin-toolbar{align-items:flex-start;gap:20px}.admin-identity summary{grid-template-columns:1fr}.login{padding:23px}footer{gap:12px;flex-direction:column}}
`;

export const APP_JS = String.raw`
const copy = async (button) => {
  await navigator.clipboard.writeText(button.dataset.copy || '');
  const old = button.textContent;
  button.textContent = '已复制'; button.classList.add('copied');
  setTimeout(() => { button.textContent = old; button.classList.remove('copied'); }, 1200);
};
document.addEventListener('click', (event) => {
  const button = event.target.closest('[data-copy]');
  if (button) copy(button).catch(() => {});
});
`;

export const ADMIN_JS = String.raw`
const $ = (selector, root = document) => root.querySelector(selector);
const escapeHtml = (value) => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const request = async (url, options = {}) => {
  const response = await fetch(url, { ...options, headers: { 'content-type': 'application/json', ...(options.headers || {}) } });
  const data = await response.json().catch(() => ({}));
  if (!response.ok) { if (response.status === 401) location.reload(); throw new Error(data.error || '请求失败'); }
  return data;
};
const toast = (message, bad = false) => {
  const element = $('#toast'); element.textContent = message; element.hidden = false; element.classList.toggle('bad', bad);
  clearTimeout(window.toastTimer); window.toastTimer = setTimeout(() => element.hidden = true, 2600);
};

const loginForm = $('#login-form');
if (loginForm) loginForm.addEventListener('submit', async (event) => {
  event.preventDefault(); const error = $('#login-error'); error.textContent = '';
  try { await request('/api/login', { method: 'POST', body: JSON.stringify({ password: loginForm.password.value }) }); location.reload(); }
  catch (reason) { error.textContent = reason.message; }
});

const app = $('#admin-app');
let state = { identities: [], audit: [] };
const keyHtml = key => '<div class="key-admin"><div><strong>' + escapeHtml(key.label || key.key_comment || key.key_type) + '</strong><code>' + escapeHtml(key.fingerprint) + '</code></div><button class="btn danger small" data-delete-key="' + key.id + '">删除</button></div>';
const identityHtml = identity => '<details class="admin-identity" data-identity="' + identity.id + '"><summary><div><strong>' + escapeHtml(identity.name) + '</strong><span>@' + escapeHtml(identity.handle) + ' · ' + identity.keys.length + ' keys</span></div><span class="visibility">' + (identity.is_public ? '公开' : '隐藏') + '</span></summary><div class="identity-editor"><form class="edit-identity form-grid"><div class="field"><label>名称</label><input name="name" maxlength="80" required value="' + escapeHtml(identity.name) + '"></div><div class="field"><label>Handle</label><input name="handle" maxlength="32" pattern="[a-z0-9][a-z0-9-]{0,31}" required value="' + escapeHtml(identity.handle) + '"></div><div class="field wide"><label>简介</label><textarea name="description" maxlength="240" rows="2">' + escapeHtml(identity.description) + '</textarea></div><div class="field"><label>可见性</label><select name="isPublic"><option value="true"' + (identity.is_public ? ' selected' : '') + '>公开</option><option value="false"' + (!identity.is_public ? ' selected' : '') + '>隐藏</option></select></div><div class="actions wide"><button class="btn primary" type="submit">保存身份</button><button class="btn" type="button" data-add-key="' + identity.id + '">添加公钥</button><button class="btn danger" type="button" data-delete-identity="' + identity.id + '">删除身份</button></div></form><div class="key-admin-list">' + (identity.keys.map(keyHtml).join('') || '<p class="empty">尚无公钥</p>') + '</div></div></details>';
const render = () => {
  $('#identity-list').innerHTML = state.identities.map(identityHtml).join('') || '<p class="empty">创建第一个身份开始使用。</p>';
  $('#identity-total').textContent = state.identities.length;
  $('#key-total').textContent = state.identities.reduce((sum, identity) => sum + identity.keys.length, 0);
  $('#audit-list').innerHTML = state.audit.map(event => '<div class="audit-item"><strong>' + escapeHtml(event.action) + '</strong><p>' + escapeHtml(event.target) + (event.detail ? ' · ' + escapeHtml(event.detail) : '') + '</p><time>' + escapeHtml(event.created_at) + ' · ' + escapeHtml(event.actor_hash) + '</time></div>').join('') || '<p class="empty">暂无操作记录</p>';
};
const load = async () => { state = await request('/api/admin/state'); render(); };

if (app) {
  load().catch(reason => toast(reason.message, true));
  $('#new-identity').addEventListener('click', () => $('#identity-dialog').showModal());
  $('#identity-dialog [data-close]').addEventListener('click', () => $('#identity-dialog').close());
  $('#key-dialog [data-close]').addEventListener('click', () => $('#key-dialog').close());
  $('#logout').addEventListener('click', async () => { await request('/api/logout', { method: 'POST', body: '{}' }); location.reload(); });
  $('#create-identity').addEventListener('submit', async event => {
    event.preventDefault(); const form = event.currentTarget;
    try { await request('/api/identities', { method: 'POST', body: JSON.stringify({ name: form.name.value, handle: form.handle.value, description: form.description.value, isPublic: form.isPublic.value === 'true' }) }); form.reset(); $('#identity-dialog').close(); await load(); toast('身份已创建'); } catch (reason) { toast(reason.message, true); }
  });
  $('#create-key').addEventListener('submit', async event => {
    event.preventDefault(); const form = event.currentTarget;
    try { await request('/api/identities/' + form.identityId.value + '/keys', { method: 'POST', body: JSON.stringify({ publicKey: form.publicKey.value, label: form.label.value, addedAt: form.addedAt.value }) }); form.reset(); $('#key-dialog').close(); await load(); toast('公钥已添加'); } catch (reason) { toast(reason.message, true); }
  });
  $('#identity-list').addEventListener('submit', async event => {
    if (!event.target.matches('.edit-identity')) return; event.preventDefault(); const form = event.target; const id = form.closest('[data-identity]').dataset.identity;
    try { await request('/api/identities/' + id, { method: 'PUT', body: JSON.stringify({ name: form.name.value, handle: form.handle.value, description: form.description.value, isPublic: form.isPublic.value === 'true' }) }); await load(); toast('身份已保存'); } catch (reason) { toast(reason.message, true); }
  });
  $('#identity-list').addEventListener('click', async event => {
    const add = event.target.closest('[data-add-key]'); if (add) { $('#create-key').identityId.value = add.dataset.addKey; $('#key-dialog').showModal(); return; }
    const key = event.target.closest('[data-delete-key]'); if (key && confirm('确认删除这把公钥？删除后公开端点将不再提供它。')) { try { await request('/api/keys/' + key.dataset.deleteKey, { method: 'DELETE', body: '{}' }); await load(); toast('公钥已删除'); } catch (reason) { toast(reason.message, true); } return; }
    const identity = event.target.closest('[data-delete-identity]'); if (identity && confirm('确认删除该身份及其全部公钥？此操作不可撤销。')) { try { await request('/api/identities/' + identity.dataset.deleteIdentity, { method: 'DELETE', body: '{}' }); await load(); toast('身份已删除'); } catch (reason) { toast(reason.message, true); } }
  });
}
`;
